targets:
  $default:
    builders:
      json_serializable:
        options:
          # DTOs mirror the DRF snake_case payloads; rename automatically so we
          # don't hand-annotate every field with @JsonKey.
          field_rename: snake
